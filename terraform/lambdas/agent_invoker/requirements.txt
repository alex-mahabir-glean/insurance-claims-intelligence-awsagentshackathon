# Powertools + Pydantic come from the AWS-managed Lambda Layer.
# boto3 is in the Lambda runtime.
# This file is for local dev / pytest only.
aws-lambda-powertools[parser]>=3.0.0
boto3>=1.34.0
pydantic>=2.0
pytest>=8.0
aws-xray-sdk>=2.14
